{
  "name": "goingsunnydatacenter",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "keystone": "^0.3.16",
    "async": "^1.5.0",
    "underscore": "^1.8.3",
    "moment": "^2.10.6",
    "cloudinary": "^1.2.4",
    "express-handlebars": "^1.1.0",
    "handlebars": "^2.0.0",
    "node-sass": "^3.3.2",
    "node-sass-middleware": "^0.9.7", 
    "dotenv": "^1.1.0"
  },
  "devDependencies": {
    "gulp": "^3.7.0",
    "gulp-jshint": "^1.9.0",
    "jshint-stylish": "^0.1.3",
    "gulp-shell": "^0.5.0",
    "gulp-watch": "^4.3.5",
    "gulp-sass": "^2.0.4"
    
  },
  "engines": {
    "node": ">=0.10.22",
    "npm": ">=1.3.14"
  },
  "scripts": {
    "start": "node keystone.js",
    "togoingsunny": "ssh -i _deploy/goingsunny.pem centos@52.221.229.162",
    "deploy_dev": "ssh -t -i _deploy/goingsunny.pem centos@52.221.229.162 'cd prod/gsun-datacenter && sudo git pull && sudo npm run build && pm2 startOrRestart pm2.json --env development'",
    "deploy_dev_hard": "ssh -t -i _deploy/goingsunny.pem centos@52.221.229.162 'cd prod/gsun-datacenter && sudo git pull && sudo npm install && pm2 startOrRestart pm2.json --env development'",
    "deploy_prod": "ssh -t -i _deploy/goingsunny.pem centos@52.221.229.162 'cd prod/gsun-datacenter && sudo git pull && sudo npm run build && pm2 startOrRestart pm2.json --env production'"
  },
  "main": "keystone.js"
}
